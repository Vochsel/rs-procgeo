{
  "name": "rs-procgeo",
  "private": true,
  "scripts": {
    "build": "scripts/build.sh",
    "build:rust": "cargo build --release && cargo test --workspace",
    "build:node": "cd bindings/procgeo-node && pnpm install && pnpm run build",
    "build:wasm": "scripts/build-wasm.sh",
    "build:python": "cd bindings/procgeo-py && maturin develop --release",
    "test": "cargo test --workspace",
    "check": "cargo check --workspace && cargo check -p procgeo-node && cargo check -p procgeo-py",
    "clean": "cargo clean && rm -rf bindings/procgeo-node/procgeo.*.node bindings/procgeo-wasm/pkg web/wasm/*.wasm web/wasm/*.js web/wasm/*.d.ts",
    "dev:web": "cd web && pnpm dev",
    "bench": "scripts/bench.sh"
  }
}
